# 新特性

## 组合式API

## Teleport

## 片段

## 触发组件选项

## 来自@vue/runtime-core的createRender API，用于创建自定义渲染器

## 单文件组件组合式API语法糖（<script setup>）

## 单文件组件状态驱动的CSS变量（<style>中的v-bind）

## SFC <style scoped>现在可以包含全局规则或只针对插槽内容的规则

